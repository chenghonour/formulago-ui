export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  BASE_HOME = '/dashboard/workbench/index',
  // initialize database page
  BASE_INITIAL_PAGE = '/init',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
  // oauth callback page
  OAUTH_CALLBACK = '/base/oauth/callback',
}
